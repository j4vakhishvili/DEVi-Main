const hand = document.querySelector(".DEVi-hand");
const handLights = document.querySelector(".Hand-Lights");
const Ball = document.querySelector(".Ball");
const DEVi = document.querySelector(".DEVi");
const Lights = document.querySelector(".Lights");
const smoke = document.querySelector(".smoke");
const bgSmoke = document.querySelector(".bg-smoke");
const stars = document.querySelector(".stars");
const sideLights = document.querySelector(".side-lights");
const topLights = document.querySelector(".top-lights");
const header = document.querySelector("header");

window.addEventListener("load", () => {
    hand.style.opacity = "100%";
    hand.style.transform = "translateY(0)";
    handLights.style.opacity = "100%";
    handLights.style.transform = "translateY(0)";
    Ball.style.opacity = "100%";
    Ball.style.transform = "translateY(0)";
    DEVi.style.opacity = "100%";
    DEVi.style.transform = "translateX(0)";
    Lights.style.opacity = "100%";
    Lights.style.transform = "translateX(0)";
    smoke.style.opacity = "100%";
    bgSmoke.style.opacity = "100%";
    stars.style.opacity = "100%";
    sideLights.style.opacity = "100%";
    topLights.style.opacity = "100%";
    header.style.opacity = "100%";
    header.style.transform = "translateX(0)";
});